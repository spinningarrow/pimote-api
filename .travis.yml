language: nix

branches:
  only:
    - master

skip_cleanup: true

jobs:
  include:
    - stage: test
      script: make test
    - stage: build
      script: make build
    - stage: deploy
      script: make deploy
